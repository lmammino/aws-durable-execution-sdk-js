# Pooya Workflow as Code Language sdk

TypeScript Language SDK for Workflow as Code

## How to test locally

### Basic example

`npm run run-locally src/demo/usage-example.ts`

### Example with retry

Run this command multiple time, until you get `COMPLETE` result

`npm run run-locally src/demo/retry-config.ts invocation001`
