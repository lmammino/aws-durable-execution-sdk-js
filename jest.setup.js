// Jest setup file to configure environment variables for tests
// This ensures verbose logging is disabled during test runs

// Disable verbose logging during tests
process.env.DURABLE_VERBOSE_MODE = "false";
